<div class="bg-white border mt-2">
  <div>
    <div class="d-flex flex-row justify-content-between align-items-center p-2 border-bottom">
      <div class="d-flex flex-row align-items-center feed-text px-2"><img class="rounded-circle" src="https://i.imgur.com/aoKusnD.jpg" width="45">
        <div class="d-flex flex-column flex-wrap ml-2">
          <span class="font-weight-bold"><%= post.title%></span>
          <span class="text-black-50 time"><%=post.author.email%></span></div>
      </div>
      <div class="feed-icon px-2"><i class="fa fa-ellipsis-v text-black-50"></i></div>
    </div>
  </div>
  <div class="p-2 px-3">
    <span><%= post.body %></span> <br>
    <%= turbo_frame_tag "like_#{post.id}" do %>
      <% like = post.find_like(current_user) %>
      <% if post.liked_by?(current_user) %>
        <%= button_to 'Delete Like', post_like_path(post, like, like: { user_id: current_user.id, post_id: post.id }), 
           method: :delete %>
      <% else %>
        <%= button_to 'like', post_likes_path(post, like: { user_id: current_user.id, post_id: post.id })%>
      <% end %>
    <% end %>
  </div>
  <div class="d-flex justify-content-end socials p-2 py-3"><i class="fa fa-thumbs-up"></i><i class="fa fa-comments-o"></i><i class="fa fa-share"></i></div>
</div>


